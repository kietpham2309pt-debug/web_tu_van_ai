# Haravan API token cho cửa hàng Bếp Ngọc Bảo (chỉ dùng khi chạy script fetch hoặc API runtime)
HARAVAN_TOKEN=53783BD6A41A168DB4E60BF1996FA57599AB64AE0AD89D3A53658C9D17DBE15D

# ── AI IMAGE PROVIDER ─────────────────────────────────────────────
# Khuyến nghị: Google Gemini (có free tier, không cần verify ID).
# Lấy key tại: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=AIzaSyCazOQuNWqH6MajVtobcttnOr5xmS-lnkE

# (Tuỳ chọn) Override model. Mặc định gemini-2.5-flash-image-preview (free).
# Đổi sang imagen-4.0-generate-001 để dùng Imagen 4 chất lượng cao hơn (trả phí).
# GEMINI_IMAGE_MODEL=imagen-4.0-generate-001

# (Tuỳ chọn) Fallback dùng OpenAI gpt-image-1 — cần verify Organization.
# Nếu set cả 2, hệ thống ưu tiên Gemini. User vẫn có thể chọn provider qua API body.
# OPENAI_API_KEY=sk-proj-xxxx

# Site URL cho SEO
NEXT_PUBLIC_BASE_URL=http://localhost:3000
